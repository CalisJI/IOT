<UserControl x:Class="WPF_TEST.View.loading_view"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_TEST.View"
             mc:Ignorable="d" 
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="CadetBlue">
        <Border BorderBrush="Black" BorderThickness="1" Background="CadetBlue">
            <Grid Background="#303030">
                <materialDesign:Card
                Background="CadetBlue"
            
                UniformCornerRadius="50">
                    <ProgressBar
                Style="{StaticResource MaterialDesignCircularProgressBar}"
                Value="50"
                Margin="0,0,0,15"
                IsIndeterminate="True" Height="50"  Width="50" />

                </materialDesign:Card>
                <TextBlock Text="Loading. . ." FontSize="30" Foreground="WhiteSmoke" Margin="0,100,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
